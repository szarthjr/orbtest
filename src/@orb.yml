version: 2.1

description: >
  Sample orb description

parameters:
  env_var:
    type: env_var_name
    default: "Teste"

steps:
  - run:
    environment:
      PARAM_TO: <<parameters.env_var>>
    name: Exemplo Parametros Orb
    command: <<include(scripts/my-script.sh)>>
